services:
  - type: web
    name: news-polar-api
    runtime: python
    plan: free
    region: frankfurt
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn src.api.app:app --host 0.0.0.0 --port $PORT
    healthCheckPath: /api/health
    envVars:
      - key: PYTHON_VERSION
        value: 3.11.9
      - key: DATABASE_URL
        sync: false
      - key: OPENAI_API_KEY
        sync: false
      # User-facing AI only (summary / bias / Q&A) — a real OpenAI key hitting
      # api.openai.com. Classify during ingestion uses the OpenRouter key on
      # GitHub Actions (OPENAI_INGESTION_*). Do not put the OpenRouter key or
      # OPENAI_BASE_URL here, or user actions would share that credit pool.
      # OPENAI_MODEL is not secret; provisioned automatically.
      - key: OPENAI_MODEL
        value: gpt-4o-mini
      # The assistant embeds the visitor's question on OPENAI_API_KEY above —
      # no extra key. Ingestion embeds the chunks through OpenRouter, and both
      # resolve to OpenAI's text-embedding-3-small, which is what makes the two
      # vectors comparable. See src/rag/embedding.py.
      - key: CORS_ORIGINS
        sync: false
