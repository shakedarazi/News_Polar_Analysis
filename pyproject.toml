[project]
name = "news-polar-analysis"
version = "0.1.0"
description = "Deterministic news article window analysis (graduation project)"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
openai = ["openai>=1.0"]
dev = ["pytest>=8.0", "openai>=1.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
