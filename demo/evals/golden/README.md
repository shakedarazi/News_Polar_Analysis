# ערכת הזהב — זוגות אירוע

## למה היא קיימת

פנקס היושרה (`demo/README.md`, סעיף 16) אומר את זה במפורש: **77 זוגות הגרסאות
שהדמו מציג הוגדרו על ידי האחזור הסמנטי עצמו**, ולכן הוא מוצא 100% מהם בהגדרה.
אי אפשר היה למדוד מהם לא דיוק ולא כיסוי, ולכן קשת הדיוק בוטלה
(`demo/HANDOFF3.md`, שורה 16).

הקובץ הזה הוא הדבר היחיד שמחזיר את שתי המדידות: **160 זוגות כתבות שנדגמו
בלי קשר לפלט של האחזור**, בשש רצועות קוסינוס — שלוש מעל הסף שבו המערכת
עובדת ושלוש מתחתיו. הרצועות מתחת לסף הן כל הסיבה שאפשר לדבר על כיסוי בכלל:
בלעדיהן מודדים דיוק וקוראים לו אחוז הצלחה.

`sample_pairs.py` מייצר את השאלות ולעולם לא את התשובות. `run_evals.py` מריץ
את הציון.

## ההגדרה שהתיוג מיישם

**`same`** — שתי הכתבות מדווחות על **אותה התרחשות מסוימת**: אותה תקרית, אותה
הודעה, אותה הצהרה, אותו פסק דין, אותו דוח. אסון נמשך שמדווח בעדכונים יומיים
נחשב התרחשות אחת.

**`not_same`** — כל השאר, **גם כשמדובר באותה פרשה**. הרצח והמעצר שאחריו הם שתי
התרחשויות. התקרית וגינויי הפוליטיקאים אליה הן שתי התרחשויות. הידיעה ופרשנות
שנכתבה עליה למחרת הן שתי התרחשויות.

הקו הזה חד יותר ממה שנשמע טבעי, ויש לו סיבה תפעולית: הדמו משתמש באשכול כדי
להשוות **מסגור** — מי מוצג כמבצע הפעולה ולמי מיוחסת אחריות — בין ערוצים. אם
כתבה אחת עוסקת בגינוי של נתניהו והשנייה בגינוי של טרופר, הפרש המסגור שיימדד
נובע מכך שמדובר בשני אנשים שונים, לא מכך שהערוצים מסגרו אותו דבר אחרת. כלל
רחב יותר היה מייצר הפרשי מסגור מזויפים.

מכריע כשקשה: **אם מוקד הידיעה של אחת הוא מוקד הידיעה של השנייה בתוספת תגובה
— `same`. אם שני המוקדים הם התרחשויות שונות — `not_same`.** כל זוג שנדרשה בו
הכרעה כזאת נושא `note` שאומר איפה נמתח הקו, כדי שאפשר יהיה לחלוק עליה במקום
לנחש אותה מחדש.

## מי תייג — וזה משנה

השדה `labelled_by` נושא `claude-opus-5` בכל 160 השורות. **זו אינה אמת אנושית
בלתי־תלויה.** מודל שפה תייג זוגות שמשמשים להערכת מודל אמבדינג — משפחות מודלים
שונות ומשימות שונות, אבל עדיין לא אדם.

לכן `run_evals.py` מדפיס את מקור התיוג לפני כל מספר, והשדה `human_reviewed`
נשאר `false` עד שכל שורה תישא `labelled_by: "human"`. עד אז המספרים הם **קו
בסיס מוצהר שממתין לסקירה**, ואסור להציג אותם על המסך כ־ground truth.

### איך סוקרים

```bash
PYTHONPATH=. python demo/evals/review.py
```

זוג אחד למסך, שתי הכותרות והפתיחים זה מול זה, ושני כפתורים. ‏`1` = אותו
אירוע, ‏`2` = לא, רווח = הבא, חץ ימינה = חזרה.

**התיוג של המודל מוסתר עד שאתם מכריעים.** כלי עם כפתור "אשר" שמוגדר מראש
לתשובה של המודל היה אוסף הסכמה שנוצרת מאינרציה — וזו בדיוק העצמאות שהערכה
הזאת צריכה כדי להיות שווה משהו. אחרי ההכרעה הכלי מגלה אם הסכמתם ומראה את
ה־`note`, ואפשר לחזור זוג אחורה ולשנות.

התור מסודר לפי מה שתיוג קונה: ‏0.90 ומעלה קובעים את הדיוק, ‏0.86-0.90 קובעת
את הכיסוי, והרצועות הדלילות רק מרחיבות חסם. אפשר לעצור אחרי 125 הזוגות
הראשונים ולקבל את שני המספרים הראשיים מתויגים אנושית. כל הכרעה נכתבת לדיסק
מיד — אפשר לסגור באמצע ולחזור.

מפני ששתי התשובות נשמרות (`label` מול `proposed_label`), הסקירה מייצרת גם
מספר שאין לנו: שיעור ההסכמה בין הסוקר למודל. ‏`run_evals.py` מדפיס אותו,
כולל הכיוון — סוקר שרק הופך ל־`not_same` קורא את ההגדרה בחומרה רבה יותר
ממה שהיא נכתבה, וזו עובדה על ההגדרה ולא על המאחזר.

## המבנה

רצועה | זוגות בקורפוס | תויגו | למה בגודל הזה
---|---|---|---
0.94-1.01 | 39 | 25 | הליבה של מה שהמערכת מקבלת
0.92-0.94 | 47 | 25 | דיוק בקצה העליון
0.90-0.92 | 126 | 30 | הרצועה שהסף החי חותך בה
0.86-0.90 | 1,784 | 45 | כאן מסתתרת החמצה — הרצועה החשובה לכיסוי
0.82-0.86 | 35,358 | 20 | חסם, לא שיעור
0.00-0.82 | 137,469 | 15 | ביקורת

הדגימה זרועה (`SEED` ב־`sample_pairs.py`), כך שאותו סנאפשוט מייצר תמיד את אותן
שאלות. ערכת זהב שחברותה משתנה בין הרצות אי אפשר לסקור פעם אחת ולסמוך עליה
אחר כך.

זוגות **חוצי מקורות בלבד** — שתי כתבות מאותו ערוץ הן המשך ולא גרסה שנייה,
ו־`_one_per_source` ב־`demo/core/framing.py` ממילא מקפל אותן.

## מה יש בקובץ

כותרות, כתובות ופתיחים של כתבות חדשות שכבר פורסמו בגלוי. **אין כאן תגובות
גולשים** — הקובץ מעוקב ב־git, בניגוד ל־`demo/data/`, ותגובות הן החלק שאסור
להיכנס לשם.
