# שכבת הדמו — נחיל סוכנים חי (Multi-Agent Demo Layer)

שכבה נפרדת לגמרי מהפייפליין הדטרמיניסטי: קוראת מ־snapshot מקומי של נתוני
המערכת, לא כותבת שום דבר בחזרה, ולא נוגעת בקוד של `src/` או `pipeline/`.
נבנתה למיצג במכללה: מסך HDMI, לולאה של כ־5 דקות, קהל עובר.

**מה הדמו מראה:** אותו אירוע חדשותי, כפי שלוש מערכות סיפרו אותו — ומה הקהל
של כל אחת עשה ממנו. השאלה "מי עוד סיקר בדיוק את האירוע הזה" היא המקום היחיד
שבו ה־AI נושא במשקל, וזה נמדד על המסך מול חיפוש מילולי.

## הפעלה

```bash
# פעם אחת, עם רשת, יום לפני המיצג:
source .venv/bin/activate
pip install -r demo/requirements.txt
PYTHONPATH=. python demo/snapshot/export_snapshot.py   # Neon → SQLite מקומי
PYTHONPATH=. python demo/snapshot/prepare_demo.py      # embeddings + אשכולות + חילוץ מסגור
PYTHONPATH=. python demo/snapshot/run_repair.py        # לולאת התיקון על מה שהמאמת פסל

# ביום המיצג (עובד גם בלי רשת):
./run_demo.sh                  # מצב מציג (HITL) — מתקדם בלחיצה
DEMO_AUTOPLAY=1 ./run_demo.sh  # לולאת קיוסק אוטונומית (בלי מציג)
```

`run_demo.sh` בונה את ה־frontend, מרים את שרת הסוכנים (‏:8010) ואת הדשבורד
(‏:3000/demo) עם watchdog שמרים כל תהליך שנפל תוך 2 שניות, ופותח Chrome במצב
kiosk. חזרות: `DEMO_SPEED=0.3 SKIP_BUILD=1 ./run_demo.sh`.

**אין מצב live/offline.** כל פלט המודל מחולץ פעם אחת ב־`prepare_demo.py`
ונשמר למטמון; בזמן המיצג לא נעשית ולו קריאת רשת אחת. התג בפינת המסך אומר
בדיוק את זה: "פלט מודל אמיתי, מוקלט מראש".

## מפת המערכת — נקודת הכניסה

המסך נפתח ב**מפה**, לא בהצגה. כל אריח הוא דלת נפרדת, והמציג נכנס לאן
שהשאלה של המבקר מובילה — בלחיצה או בהקשת הספרה של האריח. `Esc` חוזר למפה.
זה נועד לקהל של תערוכה: מי שנעצר ליד המסך שואל על דבר אחד, לא מבקש לראות
סיפור מקצה לקצה.

שני סוגי דלתות:

- **ההצגה המונחית** — רצף תשע הסצנות שלמטה, מונע מה־backend דרך SSE.
- **מודולי הסבר** (`frontend/src/components/demo/explain/`) — דיאגרמות
  סטטיות של תת־מערכת אחת, עם לשוניות פנימיות. אין בהן SSE ואין שערים.
  שמונה: **האיסוף**, **האלגוריתם**, **אחזור סמנטי**, **מסגור ואימות**,
  **אות הקהל**, **הסטטיסטיקה**, **לולאת התיקון** ו**כלכלת טוקנים**. כולם
  מוכנים; אין אריח מסומן "בבנייה".

המספרים במודולי ההסבר מגיעים מ־`demo/data/explainer_facts.json`, שנבנה על
ידי `demo/snapshot/build_explainer_facts.py` (אופליין, בלי רשת). הקובץ מפריד
בין שני סוגי עובדות:

- **קבועים** — מיובאים חיים מ־`src/` (`MAX_WINDOW_TOKENS`, `MAX_ATTEMPTS`,
  `TRACKING_PARAMS`, טבלאות התחיליות). שום מספר לא מוקלד מחדש, ולכן דיאגרמה
  על הקיר לא יכולה להתנתק מהקוד שהיא מתארת. `tests/test_explainer_facts.py`
  נכשל אם הקובץ מתיישן.
- **מדידות** — מחושבות מ־`demo/data/demo.sqlite`, אותו סנאפשוט קפוא שהקיוסק
  כבר רץ עליו. גם שתי הדוגמאות המעובדות (קריסת הכתובת המלוכלכת לאותו
  `article_id`, וחישוב הדומיננטיות על חלון אמיתי) מחושבות על ידי פונקציות
  הפייפליין עצמן — לא נכתבו ביד.

אם הקובץ חסר, המודולים עדיין נפתחים ומציגים את הדיאגרמות; רק פסי המדידה
נעלמים, וסרגל הכותרת מסמן "אין קובץ מדידות".

## שליטה ידנית (HITL) ומבנה הסצנות

ההצגה המונחית היא רצף של **9 סצנות ממוקדות**, ובסוף כל סצנה היא עוצרת
ב"שער" עד שהמציג מקיש **רווח/Enter/חץ** או לוחץ על כפתור ההמשך שעל המסך:

1. **הארכיטקטורה** — הפייפליין הדטרמיניסטי, בסדר הכרונולוגי המדויק של
   הריצה המתוזמנת ב־GitHub Actions (crawl → windows → comments → lexicon →
   analyze → Postgres), ואז שכבת הסוכנים שמעל.
2. **איסוף — עוד בלי AI** — סקאוט מביא מנה מעורבת של כתבות, ועץ ההחלטות
   לקישורים שבורים רץ בקצב אנושי. בשלב הזה עוד לא יודעים מה קשור למה.
3. **האלגוריתם — עדיין בלי AI** — לקסיקון בן שמחון על כתבה אמיתית + פאנל
   "כך זה נראה באתר Trust" עם ביאור לכל שדה (חלונות, דומיננטיות,
   audience_mean/p85).
4. **כאן נכנס ה־AI: מי עוד סיקר את זה?** — חיפוש מילולי על הכותרות מול
   אחזור סמנטי, שניהם מחושבים חי, והכותרות על המסך כדי שאפשר יהיה לבדוק.
5. **המסגור** — מודל שפה מחלץ מי מוצג כמבצע, למי מיוחסת אחריות, וקול
   פעיל/סביל, בשלוש הגרסאות זו לצד זו; ואז המאמת מדווח מה פסל.
6. **אותו אירוע, קהלים שונים** — נושא הכתבה מול נושא התגובות, קיטוב הזנב
   הקולני, והתגובה המדורגת ביותר בכל גרסה.
7. **פרופיל הערוץ** — התרחבות לכל האירועים המשותפים: סטייה מחציון האירוע
   עם רווחי סמך, פילוח לפי תחום, עקומת הדגימה וגלאי נקודת־שינוי.
8. **כלכלת טוקנים** — מה שכבת ה־AI עלתה בפועל מול אומדן "הכל ב־LLM".
9. **סיכום** — נשאר על המסך עד שמתקדמים; ההמשך מתחיל ריצה חדשה **עם סיפור
   אחר** (יש שלושה אירועי ראווה, והלולאה מתחלפת ביניהם).

ב־`DEMO_AUTOPLAY=1` כל שער ממשיך לבד אחרי ~12 שניות (לולאת קיוסק/CI);
במצב מציג השערים מחכים בלי הגבלת זמן, וה־backend **מתעלם** מבקשות
restart אוטומטיות כדי שדף ישן/תקוע לא יפיל מצגת חיה.

## מה רץ שם

| סוכן | Tier | תפקיד | האם זה AI? |
|---|---|---|---|
| 🛰️ סקאוט | 2 | איסוף; עץ החלטות לקישורים שבורים (selector → archive.org → RSS → דילוג) | לא |
| 📖 לקסי | 1 | לקסיקון הקיטוב (מחקר אלמוג בן שמחון) — ספירת מילון, בלי NLP בזמן ריצה | לא |
| 🗂️ הספרנית | 3 | אחזור סמנטי: מוצאת את הגרסאות האחרות של אותו אירוע | **כן, וזה נושא במשקל** |
| 🤖 נובה | 4 | חילוץ מסגור + ניתוח קונטרסטיבי מול הגרסאות שאותרו | **כן** |
| 🎓 עמית | 5 | מאמת: כל ביטוי נבדק מול הטקסט שהמודל קרא; מה שלא נמצא — נופל | לא — דטרמיניסטי |

**המספרים שעל המסך, כפי שנמדדו על הסנאפשוט הנוכחי:**

- ‏69 אירועים חוצי־מקורות, 8 מהם בשלושה מקורות.
- אחזור על אירוע הראווה: **0 מתוך 2** בחיפוש מילולי מול **2 מתוך 2** סמנטי.
- ‏ynet ‎+0.0173 ‏CI ‏[+0.0045,+0.0309] מובהק · mako ‎−0.0233 ‏[−0.0379,−0.0088]
  מובהק · הארץ ‎−0.0224 ‏[−0.0664,+0.0158] **לא** מובהק · news12 אירוע אחד,
  אין פרופיל.
- פילוח לפי תחום: 4 תאים מגיעים לסף הגודל, **אף אחד מהם לא מובהק**.
- גלאי נקודת־שינוי: 6 סדרות ארוכות מספיק מתוך 8, **מעבר אחד** בסף 0.05
  (‏mako, ‏p=0.0410) שנופל תחת תיקון Bonferroni; עוצמת הגילוי 47%/77%/97%
  לסטיית תקן שלמה ו־17%/26%/42% לחצי סטייה, על 20/40/75 אירועים.
- עקומת הדגימה: רוחב רווח הסמך 0.0852 → 0.0264 בין 3 ל־66 אירועים.
- המאמת: **4 מתוך 250** ביטויים טעונים, **0 מתוך 142** מבצעים, ו־**33 מתוך
  144** ציטוטי ראיה נפסלו.
- עלות כל שכבת ה־AI: **$0.0289** על 214 קריאות, פעם אחת, אופליין.

## פנקס היושרה (מה אמיתי ומה מפושט) — לקרוא לפני שמציגים

דברים שחשוב להגיד נכון אם שואלים:

1. **הסקרייפינג בזמן המיצג מדומה.** הכתבות נטענות מה־snapshot המקומי;
   תרחישי "הקישור שבור" מתוסרטים מראש כדי שעץ ההחלטות יודגם בצורה חוזרת
   ואמינה. עץ ההחלטות עצמו (הלוגיקה, הסדר, הדיווח) רץ באמת. הקרולרים
   האמיתיים של המערכת הם אלה שאספו את הנתונים מלכתחילה (`src/crawling/`).
2. **בחירת האירועים שעולים למסך היא בחירה שלנו.** שלושת אירועי הראווה
   מדורגים לפי כמה נתוני קהל יש בהם, כי אירוע בלי תגובות משאיר חצי מסך ריק.
   מה שנמדד **בתוך** אירוע לא נגוע בזה, ופרופיל הערוץ בסצנה 7 מחושב על כל
   ‏69 האירועים ולא על השלושה שנבחרו.
3. **פלט המודל מוקלט מראש.** החילוץ רץ פעם אחת מול המודל האמיתי ונשמר
   למטמון; המיצג משמיע הקלטה. הפלט אמיתי, הקריאה לא חיה. זה מה שמאפשר
   לקיוסק לעבוד בלי רשת בכלל.
4. **עמית הוא לא דעה שנייה של מודל.** הוא בדיקת עיגון מחרוזות: כל ביטוי
   חייב להימצא ב־500 התווים שהמודל עצמו קרא (`EXTRACT_LEAD_CHARS`). קבוע
   אחד משרת גם את החילוץ וגם את האימות בכוונה — כשהם נפרדו ל־500 ול־600,
   ביטוי שהופיע רק בתווים 500–600 עבר אימות למרות שהמודל לא ראה אותו.
5. **כל השוואה בין ערוצים היא מול חציון אותו אירוע.** ממוצע גולמי של ערוץ
   מודד אילו סיפורים הוא בוחר לסקר, לא איך הוא ממסגר אותם.
6. **כל ערוץ תורם גרסה אחת לאירוע** — הפופולרית ביותר בתגובות. ערוץ שפרסם
   חמישה המשכים לאותו סיפור היה אחרת מספק חמש מתוך תשע גרסאות, והחציון היה
   הופך להיות הוא עצמו.
7. **סף האשכול הוא 0.90 בדמיון קוסינוס, וזו בחירה.** הסריקה שמוצגת
   במודול "אחזור סמנטי" נמדדת מחדש בכל בנייה: 0.86 → 83 אירועים (19 עם
   שלושה ערוצים) אבל האשכולות כבר נסחפים לסיפור מתמשך שלם; 0.90 → 69 (8);
   0.92 → 50 ו**אפס** אירועים בשלושה ערוצים, כלומר ההשוואה מתה. הגבול בין
   "אירוע" ל"סיפור מתגלגל" נקבע בעין ולא במדד — הטבלה מראה מה כל ערך עולה,
   היא לא מוכיחה ש־0.90 נכון.
8. **"לא נמצא שינוי" נאמר תמיד יחד עם עוצמת הגילוי.** תא מתחת ל־10 אירועים
   לא יכול לצאת מובהק, גם אם רווח הסמך שלו במקרה לא חוצה אפס.
9. **חיווי העלות** — ‎$0.0289 הוא מדידה של הטוקנים שבאמת נצרכו בהכנה.
   האומדן שמולו ("אילו הכל היה LLM") הוא **אומדן** ומסומן ככזה על המסך.
10. **פאנל "כך זה נראה באתר" הוא שחזור נאמן של כרטיסי המוצר**, לא iframe של
    האתר החי — כדי שהקיוסק לא יהיה תלוי ברשת. כל הערכים בו אמיתיים
    מה־snapshot.
11. **‏`?mock=1` הוא מסך גיבוי.** הוא מציג את אירוע הראווה האמיתי ואת
    המספרים האמיתיים, אבל שום דבר בו לא מחושב חי — לכן הכותרת מסמנת "מצב
    הדגמה".
12. **אין בפרויקט הזה crawl4ai ואף לא framework זחילה אחר.** מסלול הכתבות
    הוא `requests` + `feedparser` + `BeautifulSoup/lxml` בלבד, בלי הרצת
    JavaScript. Playwright כן מותקן, ורץ במקום אחד ויחיד: איסוף התגובות של
    הארץ. מודול "האיסוף" מתאר את `src/crawling/` כפי שהוא כתוב, כולל
    ההשהיה הסדרתית של 2 שניות ואת העובדה שאין באטצ׳ים מקביליים בתוך מקור.
13. **הגילוי אינו זחילה על עץ הקישורים** — הוא רשימת פידים קבועה בקוד (ועבור
    רשת 13, סריקת `__NEXT_DATA__` של דף הניוזפיד). כתבה שלא עברה בפיד לא
    תתגלה. זו החלטה, וכך היא מוצגת.
14. **‏349 התווים של הארץ הם ראיה, לא כתיבה תמציתית.** אף כתבה של הארץ
    בסנאפשוט אינה עוברת 739 תווים, כי גוף הכתבה מאחורי תשלום ושתי שכבות
    החילוץ הראשונות נופלות — מה שנשמר הוא `og:description`. כל מסקנה על
    הארץ בהמשך מבוססת על פסקה אחת לכתבה, וזה מה שמסביר את רווח הסמך הרחב
    שלו. המודול אומר את זה במפורש.
15. **כל תשעת האריחים במפה פעילים** — אין יותר אריח "בבנייה". אם מוסיפים
    אריח חדש, `ready: false` מסמן אותו כמעומעם ולא לחיץ, וזו עדיין הדרך
    היחידה המותרת להציג משהו שלא נבנה: מסך שמבטיח דלת שאינה נפתחת הוא
    הדבר שהקהל זוכר.
16. **‏"17 מתוך 77" אינו ניצחון של האחזור הסמנטי על חיפוש מילולי בתנאי
    מעבדה.** 77 זוגות הגרסאות הוגדרו על ידי האחזור הסמנטי עצמו, ולכן הוא
    מוצא 100% מהם **בהגדרה**. הטענה היחידה שהמספר תומך בה: מבין הזוגות
    שהאמבדינגים מצאו, בסיס מילולי (Jaccard ≥ 0.25 על כותרות) היה משחזר 22%,
    ו־15 מהזוגות לא חולקים אף מילה. אין כאן ground truth ידני. שורת המגבלה
    ירדה מהמסך (סעיף 54), והחסם עבר לתוך המשפט עצמו: כל טענה בפאנל אומרת
    "מהזוגות שהמערכת מצאה", ולא "מכל הזוגות שקיימים".
17. **האינדקס מכסה 752 מתוך 1,178 הכתבות.** כתבה מתחת ל־400 תווים לא
    נכנסת — אין לה פתיח לעגן עליו. זה פוגע בהארץ יותר מכולם (82 מתוך 235),
    כהמשך ישיר לפיי־וול שמתואר בסעיף 14. הכיסוי לפי מקור מוצג במודול.
18. **‏`article_id = sha256(canonical_url)` היא זהות של כתובת, לא של תוכן.**
    האינדקס חשף שלושה זוגות בקוסינוס 1.0000 — אותה כתבה בדיוק תחת שני
    נתיבים באתר (`news-world` מול `news-military` במאקו, `.premium/` בהארץ,
    קידומת מדור ב־ynet). המערכת לא מאחדת אותן; כלל "גרסה אחת לכל ערוץ" הוא
    שמונע מהן להכפיל אירוע. **הפאנל שהציג את זה ירד מהמסך בסעיף 54** —
    כל תוכנו היה דיווח על מצב הנתונים, ולא החלטה. המגבלה נשארת פתוחה כאן,
    והמציג עונה ממנה כששואלים. מודול האיסוף אומר על המסך רק את מה שנכון
    לגמרי: אותה **כתובת** בשתי צורות נספרת פעם אחת.
19. **‏23% מציטוטי הראיה נפסלו — ורוב הפסילות הן שלנו, לא של המודל.** פירקנו
    את 33 הפסילות: **20** מהן זהות לטקסט מילה במילה למעט פיסוק (בדרך כלל
    נקודה שהמודל הוסיף בסוף המשפט), **2** הן ציטוט נכון עטוף בתווית שהמודל
    הוסיף, ורק **11** הן ניסוח מחדש או השמטה אמיתיים — 7.6% מ־144. **לא**
    ריככנו את הכלל בעקבות המדידה: הוא הערובה היחידה שכל ביטוי על המסך נמצא
    בטקסט כלשונו, ושתי הטעויות נופלות לאותו כיוון — פחות על המסך, לא יותר.
    זו החלטה פתוחה, והפירוק מוצג במודול.
20. **גם פסילת המילים הטעונות אינה עדות להזיה.** הדוגמה שעל המסך היא ynet:
    הפתיח אומר "הצעידה המבורכת", המודל החזיר "מברכת", והמאמת — שהוא השוואת
    מחרוזות ולא נטיות — פסל. 4 מתוך 250 מילים נפסלו, ו**אפס** מתוך 142 שמות
    מבצע.
21. **‏31% מפלטי המודל מכילים ראשי תיבות עם גרש (‎ארה"ב, בנתב"ג, הרמטכ"ל).**
    ‏66 מתוך 214 הפלטים במטמון, 33 ראשי תיבות שונים. בלי `_repair_hebrew_quotes`
    הם היו נכשלים בפענוח ה־JSON. זה נמדד מהמטמון, לא נזכר מהזיכרון.
22. **שקלול הלייקים אינרטי על מחצית מהתגובות.** ‏`engagement_weight =
    1 + ln(1 + likes + dislikes)`, אבל mako וחדשות 12 לא חושפים ספירת לייקים
    כלל — 14,405 מתוך 38,492 התגובות, ועבור 271 כתבות השקלול **לא יכול**
    לשנות דבר. מספר התגובות שמקבלות משקל 1.000 בדיוק גדול יותר, 18,526, כי
    הוא כולל גם תגובה בערוץ שכן חושף לייקים שפשוט לא קיבלה אף אחד; שני
    המספרים מוצגים בנפרד במודול מאז הכתיבה מחדש. גם היכן שיש לייקים ההזזה הממוצעת ב־p85 היא
    0.00519 על סקאלה שהחציון שלה 0.05. השקלול נשאר כי הוא נכון עקרונית, לא כי
    הוא מכריע — וכל מסקנה חייבת לעמוד גם בלעדיו. הכול נמדד ומוצג במודול.
23. **‏`controversy = 4p(1−p)` הוא מדד חי בקוד בלי נתונים.** הצינור מחשב אותו
    לכל תגובה, אבל אף מקור בסנאפשוט לא חושף דיסלייקים, ולכן `p` תמיד 1 והתוצאה
    **אפס בכל 644 הכתבות**. לא הסרנו אותו מהצינור (זה `src/`), אבל הוא מוצג על
    הקיר כמה שהוא: מדד מיושם שאין מאחוריו מדידה.
24. **‏76% מהתגובות מקבלות ציון 0.0000.** ‏29,358 מתוך 38,492 אינן מכילות אף
    מילה ממילון הקיטוב. המשמעות: האות נשען על מיעוט התגובות, ורגיש לרעש הרבה
    יותר ממה שהמספר "38,492" מרמז. הדוגמה המעובדת במודול מראה את זה במפורש —
    התגובה הכי אהודה בכתבה (28 לייקים, זועמת) מקבלת 0.0000, וכל האות שם נשען
    על שתי תגובות שכללו את "חייבים" ו"בושה".
25. **אחוזון 85 על מדגם קטן אינו אומד.** ‏58 כתבות בסנאפשוט הן בעלות פחות מ־5
    תגובות; שם p85 הוא פשוט התגובה השנייה מלמעלה. הן נספרות ככל השאר. בדוגמה
    המעובדת רואים גם את השבירות: המשקל המצטבר עצר על 23.341 מול יעד 23.459,
    ופער של פחות מעשירית משקל הזיז את התוצאה מ־0.0408 ל־0.0625.
26. **חטיפת הנושא (52 מתוך 106 גרסאות) נמדדת בלקסיקון הכתבות.** אותו מילון
    שבע־קטגוריות שנבנה לטקסט עיתונאי מופעל כאן על שפת דיבור של תגובות. זה
    שימוש מחוץ להגדרה המקורית והוא מוצהר על המסך. הפילוח לפי ערוץ **ירד
    מהמסך** בכתיבה מחדש (סעיף 52): ‏n לערוץ קטן מכדי לשאת טענה, ולכן מה
    שמדווח הוא הצירוף בלבד. תא חדשות 12, שהיה 0 מתוך 1 ומסומן "מדגם קטן מדי
    לדיווח", אינו מוצג יותר.
27. **‏62% מהשונות בדומיננטיות היא איזה סיפור סוקר, לא איך.** פירוק השונות
    על 146 גרסאות: 62% בין אירועים, 35% בתוך אירוע. לכן ממוצע גולמי לערוץ
    מודד בעיקר בחירת סיפורים. זה גם נראה בדירוג: הערוץ עם הממוצע הגולמי הנמוך
    ביותר (הארץ) אינו הערוץ עם הסטייה התוך־אירועית הנמוכה ביותר (mako).
28. **‏61 מתוך 69 האירועים הם זוגות — וזו המגבלה החמורה ביותר בשכבה הזו.**
    בזוג, החציון הוא נקודת האמצע, ולכן שתי הסטיות הן בהכרח +d/2 ו־−d/2: לא
    שתי תצפיות עצמאיות אלא השוואה אחת שנרשמה פעמיים. 40 מהזוגות הם ynet מול
    mako, ולכן "ynet טעון יותר" ו"mako טעון פחות" הם ברובם **אותו ממצא**.
    מוצג במפורש במודול.
29. **מטריצת נושא×ערוץ: 19 תאים, 4 שמישים, אפס מובהקים.** אחרי הפיצול לנושאים
    לא נשאר אף תא שגם עומד ברצפת 10 האירועים וגם הרווח שלו לא נוגע באפס. שני
    תאים כן נראים כמו ממצא (mako·פוליטיקה n=6, mako·בינלאומי n=5 — רווחים
    שלמים מתחת לאפס) ושניהם **נפסלים** על סמך n. הם מוצגים על המסך דווקא כדי
    שיראו איך נראית תוצאה שאסור לדווח.
30. **‏19 בדיקות מובהקות רצו על הקיר הזה; 3 ירדו מתחת ל־0.05; אחת שורדת
    תיקון.** ‏6 רווחי סמך ברמת ערוץ + 7 תאים שמישים + 6 סריקות נקודת שינוי.
    מספר ה"מובהקים" הצפוי מרעש בלבד הוא 0.95, וקיבלנו 3. סף Bonferroni הוא
    0.00263, ורק סטיית הדומיננטיות של mako (p מתחת ל־0.0005) עוברת אותו.
    **זה המשפט היחיד שמותר לומר מהמסך הזה.** נקודת השינוי של mako (p=0.0410)
    ורווח הסמך של ynet (p=0.0100) נופלים.
31. **טבלת העוצמה נקראת מ־`demo/data/demo_set.json`, לא מחושבת מחדש בכל בנייה**
    — היא עולה כ־20 שניות, וזו אותה טבלה שהריצה המסופרת כבר מציגה, כך שהקיר
    והריצה לא יכולים לסתור. יש טסט שמחשב שורה אחת ממנה מחדש ובודק התאמה מדויקת.
    התוכן עצמו הוא הסתייגות: בגודל הסדרות שלנו, שינוי של חצי סטיית תקן נתפס
    ב־17%–42% מהמקרים בלבד, ולכן "לא נמצאה נקודת שינוי" נקרא **"לא נמצא שינוי
    בגודל שהיינו יכולים לראות"**.

### מגבלות שחייבות להיאמר אם שואלים

- טווח הסנאפשוט הוא **חמישה ימים**. "לאורך זמן" ייבנה רק מהריצה בענן כל
  6 שעות.
- ציר הזמן הוא `first_seen_at` — מועד **סריקה**, לא פרסום, והמנה הראשונה
  היא מילוי־לאחור של מאות כתבות בשעה. ארטיפקט סריקה, לא דפוס מערכתי.
- מטריצת הכיסוי מערבבת בחירה מערכתית עם נפח סריקה: ל־channel14 יש 9 כתבות
  בסך הכל בסנאפשוט, ולכן 0% כיסוי שם הוא עובדה על הקרולר, לא על המערכת.
- **פרופיל ברמת כתב אינו אפשרי** — אין שדה מחבר בנתונים. נבדק: 4 התאמות
  מתוך 1,178, כולן false positive.
- ההבדל המצטבר בין הערוצים הוא בספרה השלישית אחרי הנקודה. **הדרמה נמצאת
  באירוע הבודד**, לא בפרופיל המצטבר.

32. **‏8 מתוך 10 שלבי המערכת לא קוראים למודל בכלל, ושני הנותרים עלו
    ‎$0.028897 בסך הכל.** הסכום עצמו לא מרשים ולא אמור להרשים — הטענה היא
    צורת החשבון, לא גודלו. השלבים בטבלה סופרים את אותם מספרים שהאריחים
    הקודמים מדדו (`build_economy` מקבל את `facts` ולא סופר מחדש), כך ששמונה
    מודולים לא יכולים לחלוק על גודל הקורפוס.
33. **שער ההמרה 2.54 תווים לטוקן נמדד, לא נזכר.** כל 214 הפרומפטים שוחזרו
    מהמטמון ומאותה תמונת מצב, וקובץ השימוש מחזיק את מספר הטוקנים האמיתי
    שחויב — חלוקה של האחד בשני נותנת את השער של הקורפוס הזה. צד הפלט נמדד
    בנפרד ויוצא 2.41; הפער (5.2%) ידוע ומוסבר על המסך: במטמון נשמרת התשובה
    לאחר פענוח, ולכן היא מעט קצרה מזו שחויבה. **כל מספר נגזר בלוח הזה משתמש
    בשער המעוגל שמוצג על המסך**, כדי שמבקר עם מחשבון יגיע בדיוק לאותה תוצאה.
34. **הפיצול בין שני סוגי הקריאות נגזר, לא נמדד.** `llm_usage.json` מחזיק
    סכום אחד. טוקני הקלט חולקו לפי התווים שנמדדו בכל סוג, וטוקני הפלט לפי
    אורך התשובות השמורות — כלומר בהנחה שאותו שער חל על שניהם. הסכום מדוד,
    החלוקה נגזרת, וכתוב כך על המסך.
35. **יום התצוגה במודול הוא הנחה מוצהרת, לא מדידה**: 8 שעות, לולאה כל 5
    דקות, 96 לולאות (`SHOW_HOURS` / `LOOP_MINUTES` ב־`build_explainer_facts.py`).
    התוצאה — ‎$0.049 ליום בלי מטמון — היא בדיוק הנקודה: **המטמון כמעט לא חוסך
    כסף**. מה שהוא קונה הוא ריצה בלי רשת ואותה תוצאה בכל לולאה, וזה מה
    שהמודול אומר במקום להתהדר בחיסכון.
36. **האומדן הנגדי ("אילו הכל היה מודל") ירד מהמסך.** לשונית "מה לא נספר"
    נמחקה כמיותרת, ואיתה פאנל האומדן. ‏$4.04 ו־$0.1692 עדיין נבנים
    ב־`build_economy` ונשמרים ב־`explainer_facts.json` — הם פשוט לא מוצגים.
    זה מוריד טענה, לא מחליש אותה: אין על הקיר מספר שאין לו הסבר לידו.
    להחזיר את הפאנל = להחזיר את שני האומדנים יחד, לא אחד מהם.
37. **החשבון הזה אינו החשבון הגדול של המערכת.** מסווג הקטגוריות של
    הפייפליין (`src/nlp/classify.py`, ‏gpt-4o-mini על 1,200 תווים ראשונים)
    רץ על **כל** כתבה כל 6 שעות — כ־$0.185 על תמונת המצב הזאת, פי 6 משכבת
    הסוכנים כולה. אחרי מחיקת לשונית ההחרגות הגילוי הזה עבר ל־`Caveat`
    בלשונית החשבון, עם המספרים של כל ההחרגות שיש להן סכום. הוא לא ירד
    מהמסך — הוא התקצר.

38. **לולאת התיקון היא שכבה שנייה, לא החלפה של המאמת.** המאמת
    הדטרמיניסטי רץ ראשון ומוחק כרגיל — `framing_dropped` ו־
    `contrast_rejected` נשארים התיעוד של מה שהחילוץ טעה בו. מה שהלולאה
    מחזירה נרשם בשדה נפרד (`contrast_repaired`, `framing_repaired`) ומסומן
    על המסך כ"הוחזר", ולא כ"היה נכון מלכתחילה". על שלושת הסיפורים המוצגים
    הוחזרו 3 ציטוטים.

39. **התקרה נמדדה ולא נוחשה, וההפרש הוא מה שזה עלה.** הלולאה רצה על
    `REPAIR_ATTEMPTS_MEASURED = 2` על כל הקורפוס: ניסיון 1 תיקן 23 מתוך 30,
    ניסיון 2 תיקן 0 מתוך 6. לכן `MAX_REPAIR_ATTEMPTS = 1` בקוד, ושש הקריאות
    של הניסיון השני הן מה שעלה לדעת את זה. `repair_log.json` שומר את הניסיונות
    שנדחו, ולא רק את המתקבלים.

40. **"תוקן" ו"הודה שאין ציטוט" נספרים בנפרד.** שתי התוצאות מנקות את ההפרה,
    ורק אחת מהן היא החזרה: 14 ציטוטים הוחזרו ומאומתים, ב־15 מקרים המודל
    החזיר `null`. סכימה של שניהם למספר אחד הייתה מנפחת את מה שהלולאה עושה.

41. **כלל אי־הנסיגה נוסף אחרי כשל אמיתי, וזה מוצג.** הגרסה הראשונה של
    הלולאה קיבלה כל תיקון שהוריד את מספר ההפרות. מחיקת ציטוט תקין אינה
    הפרה, ולכן המודל "תיקן" בכך שהחזיר `null` גם למקורות שכבר עברו — **13
    ציטוטים תקינים נהרסו**. מאז התיקון נוגע רק במקורות שנפסלו, ומתקבל רק אם
    מספר הציטוטים המאומתים לא ירד. המספר 13 הוא ממצא מהריצה הראשונה שנמחקה,
    והוא מסומן ככזה; ‏0 הוא המדידה החיה. `tests/test_repair.py` מחזיק אותו שם.

42. **התיקון עלה $0.0043 — 14.8% על גבי שכבת המודל.** 36 קריאות על 30
    פריטים. בזמן התצוגה הלולאה לא קוראת למודל: היא מנגנת את
    `repair_cache.json`. חסר קובץ או אין רשת — הפריט חוזר בדיוק למצב שאחרי
    המאמת, ציטוט ריק, אף פעם לא ציטוט שגוי.

43. **מודול כלכלת טוקנים אוחד מ־6 לשוניות ל־4, וחלק מהתוכן נמחק.** הכלל
    שנכתב ב־`demo/WRITING.md` הוא פאנל אחד = החלטה הנדסית אחת, ולכן מה
    שלא החזיק טענה נפרדת ירד: פאנל שלוש הדוגמאות לשער ההמרה, פאנל "שלושת
    הברזים" (חזר על תוכן משתי לשוניות אחרות), ודיאגרמת קבצי המטמון עם
    כרטיס המדד שלה. הטענות עצמן לא נחלשו — השער נמדד ומוצג ב־hint
    וב־`Caveat` של לשונית 2, וטענת המטמון (ריצה בלי רשת ותוצאה זהה, לא
    חיסכון בכסף) עברה ל־`Caveat` בלשונית החשבון יחד עם הנחת יום התצוגה
    (סעיף 35). היקף הטקסט במודול ירד מ־841 מילים ל־395.

44. **האינדקס לא מתיישן, כי אין לו חלון וגם לא מדיניות פינוי.** הוא מחזיק
    את כל 752 הווקטורים, 1.1MB, שאילתה ב־0.0098ms. על 4.8 ימי קורפוס זו
    ההחלטה הנכונה, אבל היא החלטה ולא מקריות, ולכן נמדד מה היה קורה אחרת:
    הקורפוס מושמע מחדש בסדר ההגעה האמיתי דרך אינדקס של K סלוטים בארבע
    מדיניויות. ‏`retrieval.slots` מחזיק את הטבלה, אבל **היא כבר לא על המסך**
    (סעיף 54) — היא נמדדת, נבדקת, ונאמרת בעל פה.

45. **החלון נקבע לפי קבוע הזמן של החדשות, לא לפי זיכרון.** חצי מהאירועים
    חוצי־הערוצים נסגרים תוך 3.9 שעות ושלושה רבעים תוך 6.7, אבל הזנב ארוך —
    p90 הוא 75.5 שעות. בקצב של 156 כתבות מאונדקסות ליום, חלון של 24 שעות
    (‏156 סלוטים) מכסה 79.7% מהאירועים ו־48 שעות (312 סלוטים) מכסות 88.4%.
    ‏100% דורש שבוע, שהוא יותר מכל הקורפוס הנוכחי.

46. **FIFO ו־LRU יצאו זהים בכל K שנמדד, ולכן LRU מיותר כאן.** אין דפוס
    גישה חוזרת בזרם חדשות — פריט נכנס, נשאל לכל היותר פעם אחת, ולא נוגעים
    בו שוב, ולכן "הכי מזמן שהיה בשימוש" מתנוון ל"הנכנס הראשון". ‏LFU גרוע
    יותר (35 מול 45 מתוך 77 ב־K=50) כי הוא מקבע כתבות ישנות ומרעיב טריות,
    וזה בדיוק הכיוון ההפוך ממה שחלון חדשות צריך. הזרוע האקראית ממוצעת על
    20 ריצות, כי בזרוע סטוכסטית ריצה אחת היא אנקדוטה. גם המסקנה הזאת ירדה
    מהמסך בסעיף 54: היא נכונה, אבל היא תשובה לבעיה עתידית.

47. **`VectorIndex.add()` ו־`reset_to_base()` הן קוד מת.** התיעוד מדבר על
    "RAG מצטבר" שבו סיווגים מאושרים הופכים להקשר לסיבובים הבאים, אבל אף
    קורא לא נמצא בקוד — האינדקס נבנה במלואו ב־`prepare_demo` ולא משתנה
    בזמן ריצה. זה לא מוצג על המסך כיכולת.

48. **מודול האחזור נכתב מחדש לפי `demo/WRITING.md`, ולשוניות ירדו מ־5 ל־3.**
    כל לשונית מחזיקה שתי טענות: הבסיס המילולי שנמדד והפסיד (עם ההליכה על
    אירוע אחד כראיה), מה האינדקס עולה (עם זוגות ה־URL הכפולים שהוא חשף),
    ושני הגבולות שנמדדו — הסף והחלון. מה שירד מהמסך: היסטוגרמת הקוסינוס של
    282,376 הזוגות (טענת האחוזון נשארה במשפט אחד ובכותרת), גרף הכיסוי לפי
    מקור (הכיסוי נאמר במספרים — 752 מתוך 1,178, ובהארץ 82 מתוך 235), צ'יפ
    האחוזונים לכל סף, ותווית הנושא של אירוע הדוגמה. אף טענה לא נחלשה, ואף
    מספר לא הוקלד ביד. היקף הטקסט ירד מ־864 מילים ל־415. ‏`retrieval.slots`
    (סעיפים 44–46) מוצג עכשיו במלואו: טבלת ארבע המדיניויות מול שישה גדלי
    אינדקס.

49. **מודול האיסוף נכתב מחדש לפי `demo/WRITING.md`, נשאר בארבע לשוניות,
    ו־12 פאנלים ירדו לשמונה.** הגילוי (רשימת פידים קבועה מול זחילה, ולצידה
    התשואה בפועל לפי מקור), מפתח הזהות, סולם החילוץ (‏`requests` ו־`lxml` בלי
    הרצת JavaScript, ומולו ראיית הפיי־וול של הארץ), והריצה. **חדש על המסך:**
    התזמון גר ב־`.github/workflows/ingestion.yml` — runner חינמי, ‏cron כל שש
    שעות, והרצה ידנית ב־`workflow_dispatch` — אחרי ששני מנגנונים קודמים כשלו:
    ‏cron של מערכת ההפעלה ש־macOS חסם בשקט, ו־APScheduler שרץ מתוך ה־FastAPI
    וחייב את שרת ה־API להישאר ער רק כדי להפעיל טיימר. מכיוון שהתזמון כבר לא
    יושב בתהליך ה־API, שרת ה־Render חופשי להירדם בלי שהנתונים יתיישנו.
    מחרוזת ה־cron מוצגת כליטרל מקובץ ה־workflow ולא מ־`explainer_facts.json`,
    כי אין לה שדה עובדות — אם התזמון ישתנה היא לא תזוז מעצמה. **מה שירד
    מהמסך:** רשימת עשרת פרמטרי המעקב המחוקים (מספרם נשאר על החץ בשרשרת
    הקנוניזציה, והכתובת המלוכלכת בדוגמה עדיין מציגה `utm_source` ו־`fbclid`),
    ותיבת `seen: set`. **שני תיקוני דיוק:** ‏"התראה נשלחת" הפך ל"שורה ביומן",
    כי `check_failure_rate_spike` כותב `logger.warning` ואינו שולח דבר —
    הפאנל סתר את ה־`Caveat` של עצמו; ו"המזהה נשאר תפוס" הפך ל"תפוס עד סוף
    הריצה", כי כתבה שנכשלה אינה נשמרת ולכן היא מתגלה שוב בריצה הבאה. היקף
    הטקסט ירד מ־629 מילים ל־502.

50. **מודול האלגוריתם נכתב מחדש לפי `demo/WRITING.md`, ולשוניות ירדו מ־6
    ל־4.** מילון דטרמיניסטי במקום מודל (והמחיר: 39% מהחלונות בלי אף מילת
    לקסיקון, שנשמרים NULL ולא אפס), הרחבת התחיליות שרצה בבנייה ולא בזמן ריצה,
    החיתוך למשפט עם תקרת 60 טוקנים, והדומיננטיות כיחס בתוך החלון. **מה שירד
    מהמסך:** לשונית "מדדי הקהל" כולה — שישה כרטיסי מדד על התגובות שמודול "אות
    הקהל" מחזיק במלואם, ולכן `facts.comments` לא נקרא יותר מכאן; לשונית
    הניקוי עם שרשרת הנרמול והצגת החלון גולמי מול מנורמל; וכרטיסי המדד של
    `active` ו־`window_len`. **שתי טענות תוקנו:** הכרטיס הסביר ש־0.5 הוא
    "פיצול שווה בין שתי קטגוריות", בעוד שהחלון שעל אותו מסך יוצא 0.5 עם
    **שלוש** קטגוריות פעילות; והפאנל על התנגשות טען שצורה שמיוצרת בשתי
    קטגוריות נמחקת מהמילון, בעוד ש־`build_article_lexicon` משתמש ב־`setdefault`
    ו־101 צורות נרשמות לקטגוריה בעלת המספר הנמוך. צ'יפ "צמד מאושר" ירד עד
    שהבנאי תוקן (ראו את הקומיט על `expand_lexicon` מול `load_lexicon`), והמסך
    אומר "רשימת הצמדים סגורה" בלי למנות אותה. היקף הטקסט ירד מ־929 מילים
    ל־422.

51. **מודול המסגור נכתב מחדש לפי `demo/WRITING.md`, ולשוניות ירדו מ־5 ל־4.**
    למה מסגור משלם ומה חוזר בחמשת השדות, למה `json_object` של הפייפליין לא
    מספיק לשכבת הסוכנים (פרסור סובלני ואז אימות דטרמיניסטי) יחד עם תיקון
    הגרש בראשי תיבות, קבוע החלון היחיד ושיעורי העיגון, ופירוק הפסילות. **מה
    שירד מהמסך:** פאנל הפרומפט המלא (`framing_system`), הדוגמה הקונטרסטיבית
    (`contrast_example`) — שמוצגת חיה ב־`FramingScene` — והיסטוגרמת המילים
    הטעונות לכתבה, שהצטמצמה למספר אחד (22 כותרות בלי אף מילה טעונה). **שני
    מספרים שהיו מוקלדים ביד נמחקו:** ‏"33% עיגון מול 93%" אינו ב־`explainer_facts.json`
    ואין לו מקור, ו־600 הוחלף בניסוח יחסי מול `EXTRACT_LEAD_CHARS`. תוקנה גם
    טענת סיבה שגויה — ההתאמה הרופפת למבצע אינה בגלל הבדלי כתיב בין ערוצים,
    אלא בגלל שהמודל מחזיר שם מלא יותר ממה שהכתבה עצמה כתבה. ‏4 מתוך 250, 0
    מתוך 142, 33 מתוך 144 והפירוק 20/2/11 כולם נשארו. היקף הטקסט ירד מ־731
    מילים ל־567.

52. **מודול "אות הקהל" נכתב מחדש לפי `demo/WRITING.md`, ולשוניות ירדו מ־6
    ל־4.** שיעור ולא ספירה (והמחיר הוא מכנה של אחת — 124 תגובות בציון
    מקסימלי, 111 מהן במילה אחת), משקל לוגריתמי ולא ליניארי, אחוזון 85 ולא
    ממוצע (חציון 0.0500 מול 0.0218), ומה שהמספר מרשה לומר. הדוגמה המעובדת
    הפכה לפאנל הראיה של אחוזון 85 במקום לשונית משלה, ושלוש ההסתייגויות שהיו
    לשונית נפרדת חזרו כ־`Caveat` בפאנלים שהן שייכות להם. **מה שירד מהמסך:**
    טבלת הסטייה התוך־אירועית לפי ערוץ (אותה מדידה מוצגת במודול הסטטיסטיקה עם
    רווחי סמך ומובהקות, וכלל "מול חציון האירוע" נשאר כ־`Caveat`), היסטוגרמת
    אחוזון 85 ברמת הכתבה (הטענה נשארת במספר — 146 כתבות ב־0), טבלת חטיפת
    הנושא לפי ערוץ (ראו את התיקון לסעיף 26), וטבלת עשר התגובות של הדוגמה.
    **מספר שגוי תוקן:** כרטיס אחוזון 85 הציג "0.06 — החציון בסנאפשוט" בעוד
    החציון המדוד הוא 0.0500, וכרטיס הממוצע הציג 0.02 מול 0.0218; שניהם
    נקראים עכשיו מהנתונים. גם 3.398 ו־8.511, שהיו טקסט קבוע, מגיעים
    מ־`weight.curve`. היקף הטקסט ירד מ־891 מילים ל־539.

53. **מודול הסטטיסטיקה נכתב מחדש לפי `demo/WRITING.md`, ולשוניות ירדו מ־6
    ל־4.** הסטייה מחציון האירוע במקום ממוצע גולמי (דירוג שתי השיטות הוא
    הראיה), הבוטסטראפ ומה שהוא מסרב לענות עליו, מבחן התמורות שנאמר תמיד יחד
    עם טבלת העוצמה, ורצפת התאים לצד תיקון Bonferroni והמשפט היחיד שמותר
    לומר. **מה שירד מהמסך:** טבלת "מי נפגש עם מי" (טענת הזוג הדומיננטי נשארה
    ב־`Caveat` שליד רווחי הסמך — 40 מתוך 61 הזוגות הם ynet מול mako), רשימת
    עשרת התאים ובמקומה שני התאים שנפסלו ושלושת המספרים 19 / 4 / 0, כרטיס
    המדד של הסטייה שהנוסחה שלו נשארה בשורה אחת, ודיאגרמת שלושת השלבים של
    הבוטסטראפ. כל טענה מוגבלת־פנקס נבדקה אחת־אחת ונשארה בעוצמתה: "לא נמצא
    שינוי" הוא עכשיו כותרת הפאנל שמעל טבלת העוצמה, רצפת 10 האירועים פוסלת 15
    מ־19 התאים, ו־null אף פעם לא מוצג כאפס. **תוקן דרך אגב:** ‏p שיוצא אפס
    הוצג "‎<0.0003" — מספר מוקלד, וחזק ממה ש־4,000 דגימות יכולות לפתור. הוא
    נגזר עכשיו ממספר הדגימות ומוצג "‎<0.0005", עדיין מתחת לסף Bonferroni, ולכן
    הממצא של mako שורד כמקודם; סעיף 30 עודכן בהתאם. היקף הטקסט ירד מ־876
    מילים ל־541.

54. **מודול האחזור עבר לרגיסטר החדש (`demo/WRITING.md`), ולשוניות ירדו
    מ־3 ל־2.** נשארו שתי שאלות: איך יודעים ששתי כתבות מספרות אותו סיפור, ומה
    זה עולה להריץ. **מה שירד מהמסך:** ארבע שורות `Caveat`; הפאנל של שלושת
    זוגות ה־URL הכפולים (סעיף 18) — כל תוכנו היה דיווח על מצב הנתונים ולא
    החלטה; ו**כל שחזור הפינוי החסום** (סעיפים 44–46) — טבלת ארבע המדיניויות
    מול שישה גדלי אינדקס, חלונות הטריות, וההשוואה FIFO/LRU/LFU/RR. הוא מדד
    בעיה שלקורפוס הזה עוד אין, ולשונית של תרחישים היא נפח ולא עומק.
    ‏`retrieval.slots` ממשיך להיבנות ולהיבדק, והמציג עונה ממנו כששואלים על
    קנה מידה. גם `corpus.too_short` ירד — הכיסוי נאמר עכשיו כ־752 מתוך 1,178.
    **מה שהוחלף בשפה קונקרטית:** "זוגות" הפכו ל"מקרים שבהם שני ערוצים סיקרו
    את אותו אירוע", ו"חיפוש מילולי" הפך ל"להשוות את המילים בכותרות"; ‏`cos`
    ו־`J` הפכו ל"קרבה" ו"מילים"; ‏"‏752 ווקטורים / 384 ממדים" הפכו ל"כתבות
    באינדקס" ו"מספרים לכל כתבה"; החפיפה החציונית מוצגת כאחוז ולא כ־0.0952.
    **מה שנוסף:** הפאנל שאומר שהמודל רץ כאן ולכן אין חשבון — הדרג הנכון
    לשימוש הזה, מול API בתשלום שגובה על כל בנייה מחדש של האינדקס.
    **תוקן דרך אגב — פריסה:** כל פאנל נמתח עד לגובה הקיר, כך שפאנל עם 220px
    תוכן החזיק 590px ריק. הפאנלים בלשונית סוגרים עכשיו על התוכן שלהם והבלוק
    ממורכז (`Stage`), והטיפוגרפיה עלתה ל־18px גוף ו־38px למספרי הכותרת. שאר
    המודולים עדיין במצב הישן — שם הפאנלים מתמלאים ב־17%–59%, וזה מתוקן
    בסבב שלהם.

55. **מודול האלגוריתם עבר לרגיסטר החדש, ולשוניות ירדו מ־4 ל־2.** נשארו שתי
    שאלות: למה מילון ולא מודל, ואיך נמדדת כתבה. **מה שירד מהמסך:** שתי שורות
    `Caveat`; שרשרת החצים של בניית המילון (`למות → צורות → sha256`) — היא
    תיארה מנגנון ולא טענה; היסטוגרמת הדומיננטיות (‏6 עמודות) — הטענה שהיא
    נשאה, ‏"‏1.0 יכול להיות מילת לקסיקון אחת", נאמרת עכשיו במשפט; והספירה של
    ‏1,168 החלונות עם שלוש קטגוריות ומעלה. ‏`windows.active_hist` ו־`windows.max_len`
    כבר לא נקראים מהמודול. **מה שהוחלף בשפה קונקרטית:** "חלון" הפך ל"יחידת
    מדידה" בכל מקום שבו הקורא לא ראה עדיין את החיתוך; "למות" הפכו ל"מילות
    בסיס"; "לקסיקון" ל"מילון"; ותוויות `c1`–`c7` בגרף הקטגוריות הוחלפו במספר
    בלבד, כי שם הנושא כבר עומד לצידן. **מקראות שנוספו** (הכלל החדש
    ב־`WRITING.md`): מתחת לגרף ההרחבה, מתחת לגרף שבעת הנושאים, מתחת לרשימת
    המשפטים, ולצד המילים הצבועות בחלון. **שתי הבהרות שהיו קודם ב־`Caveat`
    ועברו לתוך המשפט:** רשימת המשפטים מציגה 6 מתוך 14 ואומרת זאת, ו־231
    היחידות שיוצאות מעט מעל תקרת 60 מוסברות בשורת המקרא (הספירה הסופית נעשית
    אחרי הניקוי). הטענה על התנגשות קטגוריות (סעיף 50) ירדה מהמסך ונשארת כאן.
    היקף הטקסט ירד מ־422 מילים ל־517.

56. **‏"גם בטמפרטורה 0 מודל שפה אינו דטרמיניסטי" הוא טענה על שירותי הסקה
    מנוהלים, ולא מדידה שלנו.** בטמפרטורה 0 הדגימה עצמה חמדנית, אבל ההסתברויות
    שממנה בוחרים מחושבות בחומרה מקבילית שבה סדר צבירת החיבורים תלוי בגודל
    האצווה — ובאיזו חברה של בקשות אחרות הבקשה שלנו עובדה. חיבור בנקודה צפה
    אינו אסוציאטיבי, ולכן הפרש בסיבית האחרונה יכול להפוך את הדירוג בין שתי
    מילים כמעט־שוות, ומשם המשפט מתפצל. במודלי mixture-of-experts הניתוב בין
    המומחים מוסיף תלות נוספת בהרכב האצווה. זו התנהגות ידועה של הסקה מנוהלת
    בקנה מידה, לא ליקוי בספק מסוים ולא משהו שמדדנו על הקורפוס הזה — **המסך
    אומר "יכולה לחזור עם תשובה אחרת", לא "חוזרת".** התוקף של הטענה כאן הוא
    בכיוון שלה: היא מסבירה למה מדד שנועד למחקר יושב בקוד דטרמיניסטי, והיא
    לא משמשת כדי לפסול את שכבת המודל — שם בדיוק בגלל זה יש מטמון, אימות
    מחרוזות, ולולאת תיקון.

57. **מודול המסגור עבר לרגיסטר החדש, ולשוניות ירדו מ־4 ל־2.** נשארו שתי
    שאלות: למה משלמים על מודל דווקא כאן, ואיך יודעים שהוא לא המציא. **מה
    שירד מהמסך:** שתי שורות `Caveat`; שלוש קופסאות ה־`Node` של הפרסור הסובלני
    (גדרות קוד, המחרוזת "null", ‏`voice` לא חוקי) — תיאור מנגנון שהטענה שמעליו
    כבר אומרת; שורת ה־`CodeRef` הכפולה; ופירוק ההתאמה למבצע (139 מלאה מול 3
    ברמת מילה), שהוא דיווח על מצב הנתונים ולא החלטה — הפס "‏0 מתוך 142" נשאר.
    ‏`verifier.actors_exact` ו־`actors_word_level` כבר לא נקראים מהמודול.
    **שתי טענות שנוסחו מחדש כהחלטה במקום כהיסטוריה:** קבוע החלון היחיד נאמר
    עכשיו כ"הבדיקה קוראת בדיוק את מה שהמודל קרא; חלון רחב יותר היה מאשר ביטוי
    שהמודל מעולם לא ראה", בלי לספר שזה קרה לנו; ותיקון הגרש בראשי התיבות נאמר
    כהחלטה בטוחה ("גרש שיש אות עברית משני צדיו לעולם אינו תוחם") ולא כדוח
    תקלה. **טענה שהודקה:** המסך אמר "‏33 מהתשובות מכילות ראשי תיבות עם גרש",
    אבל אחת הדוגמאות בקובץ (`ב"יריית`) אינה ראשי תיבות אלא תו ציטוט. מה שנספר
    בפועל הוא גרשיים בתוך מילה, וזה מה שכתוב עכשיו — "כמעט תמיד ראשי תיבות" —
    והדוגמאות המוצגות מסוננות לאלה שהגרש בהן אינו אחרי אות אחת. **מקראות
    שנוספו:** מתחת לשלושת פסי הפסילה, מתחת לגרף סיבות הפסילה (ירוק = הכלל תפס
    בעיה אמיתית, אדום = הכלל היה קשוח מדי), ומתחת לשלושת כרטיסי ההתפלגות.
    **הבהרה שנוספה כדי למנוע חשד בבאג:** ‏"‏4 בקול סביל" ו"‏4 בלי מבצע מזוהה"
    הם אותן ארבע כותרות, והמסך אומר את זה. `term_example` (מקרה ״מברכת״)
    עבר מלשונית העיגון ללשונית הבדיקה, ומופיע עכשיו מיד אחרי המשפט על כיוון
    הטעות — כדוגמה קונקרטית לכך שהמאמת משווה מחרוזות ולא נטיות. היקף הטקסט
    ירד מ־567 מילים ל־551.

## ארכיטקטורה

```
demo/
  snapshot/   export_snapshot.py (Neon→SQLite),
              prepare_demo.py (אינדקס + אשכולות + חילוץ מסגור מאומת → demo_set.json),
              run_repair.py (לולאת התיקון → repair_cache.json + repair_log.json)
  core/       framing.py (אשכולות, סטייה תוך־אירועית, bootstrap, גלאי שינוי,
                          חילוץ LLM עם מטמון, מאמת, לולאת תיקון),
              events.py (SSE broker), agent.py, index.py (אינדקס וקטורי),
              control.py (בקר השערים)
  roles/      agents.py — חמשת הסוכנים
  runner.py   מכונת הסצנות (9 סצנות, שערי HITL, סיכום, איפוס)
  server.py   FastAPI: ‏/events (SSE), ‏/state, ‏/control/advance, ‏/control/restart
  EVENTS.md   חוזה האירועים מול הדשבורד
frontend/src/app/demo/  דף הקיוסק (Next.js)
.github/workflows/agent_benchmark.yml  בנצ'מרק חינמי ב־CI
```

הערה לגבי האחזור: האינדקס הוא מטריצת numpy מנורמלת עם דמיון קוסינוס — לא
שרת vector-DB. בגודל הזה (752 כתבות) זה מהיר יותר, דטרמיניסטי, ואין לו מה
ליפול מול קהל. ה־embeddings אמיתיים (multilingual-e5-small, מחושבים מראש).

הערה לגבי החילוץ: ראשי תיבות בעברית נכתבים עם גרש כפול בתוך המילה (שב"כ,
צה"ל), והמודל פולט אותו בלי escape — מה ששובר את ה־JSON שהוא יושב בתוכו.
זה היה שליש מכשלי החילוץ עד שטופל (`_repair_hebrew_quotes`).

## צ'ק־ליסט חזרה גנרלית (יום לפני)

1. `pip install -r demo/requirements.txt` בסביבה הוירטואלית.
2. עם רשת: `PYTHONPATH=. python demo/snapshot/export_snapshot.py` ואז
   `PYTHONPATH=. python demo/snapshot/prepare_demo.py` — לוודא שההדפסה
   מסתיימת ב־`demo set written` ושמספר הכשלים בחילוץ הוא 0.
3. `DEMO_SPEED=0.02 PYTHONPATH=. python demo/benchmark.py` — חייב לצאת
   בהצלחה (זו בדיוק ריצת ה־CI: סדר הסצנות, הצטמצמות רווח הסמך, ניצחון
   האחזור הסמנטי על חיפוש המילים, והיקף עבודת המאמת).
4. חזרה מלאה בקצב מהיר: `DEMO_SPEED=0.3 SKIP_BUILD=1 ./run_demo.sh`,
   לעבור את כל 9 הסצנות עם המקלדת.
5. **לסגור כל טאב ישן של ‎/demo** מהפעלות קודמות (גם בחלונות כרום אחרים!)
   — דף ישן שמחובר ל־backend עלול לשלוח פקודות מתקופת קוד קודמת.
6. `PYTHONPATH=. python demo/snapshot/build_explainer_facts.py` — ‏`run_demo.sh`
   עושה את זה לבד, אבל כדאי לראות את ההדפסה: אם `audience:` מדווח שמדד
   המחלוקת חדל להיות אפס או שהתגובה המובילה בדוגמה קיבלה ציון חיובי,
   שני פאנלים במודול "אות הקהל" מספרים סיפור שכבר לא נכון (יש טסטים שנופלים
   על זה).
7. לעבור על תשעת האריחים במפה (1–9) ולוודא שכל הלשוניות נפתחות ושאין
   פאנל שמציג "אין קובץ מדידות". שימו לב במיוחד לשורת `economy:` בהדפסה של
   שלב 6: אם מספר הקריאות אינו שווה למספר הפריטים במטמון, הטענה "כל קריאה
   ששולמה נשמרה" נשברת ויש טסט שנופל על כך.
8. ביום עצמו: `./run_demo.sh` (מצב מציג). **קליק אחד על המסך** אחרי שהדף
   עולה — כדי שהמקלדת תיקלט בחלון הקיוסק.

## מה עושים אם X נופל (ביום המיצג)

- **הדף מציג "מצב הדגמה"** — ה־backend לא נגיש. ה־watchdog של
  `run_demo.sh` אמור להרים אותו תוך שניות; הדף חוזר ל־live לבד (בדיקה כל
  ‎15 שניות). אם לא — ‎Ctrl+C ל־`run_demo.sh` ולהריץ שוב עם `SKIP_BUILD=1`.
- **המקלדת לא מתקדמת** — קליק אחד על המסך (פוקוס), או ללחוץ ישירות על
  כפתור ההמשך. תמיד אפשר: `curl -X POST localhost:8010/control/advance`.
- **הדמו נראה תקוע באמצע סצנה** (לא בשער) — `curl -X POST
  localhost:8010/control/restart` יעבוד רק בקיוסק; במצב מציג פשוט להרוג את
  תהליך `demo/server.py` — ה־watchdog מרים אותו נקי תוך 2 שניות והדף מתחבר
  מחדש לריצה חדשה.
- **אין רשת בכלל** — לא משנה. הדמו לא פונה לרשת בשום שלב.
- **אין קול/אין עכבר/אין כלום** — ריצת `?mock=1` בדפדפן
  (`localhost:3000/demo?mock=1`) מציגה תסריט מלא גם בלי backend בכלל.
